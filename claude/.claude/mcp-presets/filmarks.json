{
  "_meta": {
    "description": "Filmarks - 映画レビュー・評価取得",
    "tags": ["movie", "review", "entertainment"],
    "requiredEnvVars": ["FILMARKS_USER_ID", "FILMARKS_SERVER_PATH"]
  },
  "type": "stdio",
  "command": "${NODE_PATH}",
  "args": ["${FILMARKS_SERVER_PATH}"],
  "env": {
    "FILMARKS_USER_ID": "${FILMARKS_USER_ID}"
  }
}
